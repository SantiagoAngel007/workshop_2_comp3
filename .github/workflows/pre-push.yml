name: Pre-Push Validation

# Este workflow valida cambios antes de que se hagan push
on:
  push:
    branches: [dev, develop, feature/*, fix/*, hotfix/*]
  pull_request:
    branches: [main, develop, dev]

jobs:
  # Validación rápida para branches de desarrollo
  quick-validation:
    name: Quick Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: |
          echo "🔍 Running TypeScript type check..."
          npm run build --noEmit || npx tsc --noEmit

      - name: Lint check
        run: |
          echo "🧹 Running ESLint..."
          npm run lint

      - name: Quick tests (unit tests only)
        run: |
          echo "🧪 Running quick unit tests..."
          npm run test -- --passWithNoTests --testPathIgnorePatterns="e2e"
        env:
          NODE_ENV: test

  # Validación completa para PRs importantes
  full-validation:
    name: Full Validation
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run full test suite
        run: npm run test:cov
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
          JWT_SECRET: test-jwt-secret-key-for-ci

      - name: Check test coverage requirements
        run: |
          echo "📊 Checking coverage requirements for PR..."
          
          if [ ! -f coverage/coverage-summary.json ]; then
            echo "❌ No coverage report found!"
            exit 1
          fi
          
          STATEMENTS=$(cat coverage/coverage-summary.json | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).total.statements.pct)")
          BRANCHES=$(cat coverage/coverage-summary.json | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).total.branches.pct)")
          FUNCTIONS=$(cat coverage/coverage-summary.json | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).total.functions.pct)")
          LINES=$(cat coverage/coverage-summary.json | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).total.lines.pct)")
          
          echo "Current coverage:"
          echo "  📝 Statements: ${STATEMENTS}%"
          echo "  🌿 Branches:   ${BRANCHES}%"
          echo "  🎯 Functions:  ${FUNCTIONS}%"
          echo "  📏 Lines:      ${LINES}%"
          
          # Verificar si cumple con los requisitos mínimos
          FAILED=false
          
          if (( $(echo "$STATEMENTS < 85" | bc -l) )); then
            echo "❌ Statements coverage below 85%"
            FAILED=true
          fi
          
          if (( $(echo "$BRANCHES < 83" | bc -l) )); then
            echo "❌ Branches coverage below 83%"
            FAILED=true
          fi
          
          if (( $(echo "$FUNCTIONS < 85" | bc -l) )); then
            echo "❌ Functions coverage below 85%"
            FAILED=true
          fi
          
          if (( $(echo "$LINES < 85" | bc -l) )); then
            echo "❌ Lines coverage below 85%"
            FAILED=true
          fi
          
          if [ "$FAILED" = true ]; then
            echo ""
            echo "💥 PR does not meet coverage requirements!"
            echo "Please add more tests before merging."
            exit 1
          else
            echo ""
            echo "✅ Coverage requirements met! PR is ready for review."
          fi

  # Verificar que no hay conflictos de merge
  merge-check:
    name: Merge Conflict Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for merge conflicts
        run: |
          echo "🔀 Checking for merge conflicts..."
          
          # Fetch the target branch
          git fetch origin ${{ github.base_ref }}
          
          # Try to merge (dry run) to check for conflicts
          if git merge-tree $(git merge-base HEAD origin/${{ github.base_ref }}) HEAD origin/${{ github.base_ref }} | grep -q "<<<<<<< "; then
            echo "❌ Merge conflicts detected!"
            echo "Please resolve conflicts before merging."
            exit 1
          else
            echo "✅ No merge conflicts detected."
          fi

      - name: Check PR size
        run: |
          echo "📏 Checking PR size..."
          
          # Count changed files and lines
          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | wc -l)
          ADDED_LINES=$(git diff --stat origin/${{ github.base_ref }}...HEAD | tail -1 | grep -o '[0-9]* insertion' | cut -d' ' -f1 || echo "0")
          DELETED_LINES=$(git diff --stat origin/${{ github.base_ref }}...HEAD | tail -1 | grep -o '[0-9]* deletion' | cut -d' ' -f1 || echo "0")
          
          echo "📊 PR Statistics:"
          echo "   Files changed: $CHANGED_FILES"
          echo "   Lines added: $ADDED_LINES"
          echo "   Lines deleted: $DELETED_LINES"
          
          if [ "$CHANGED_FILES" -gt 50 ]; then
            echo "⚠️ Large PR detected ($CHANGED_FILES files). Consider breaking it down."
          elif [ "$ADDED_LINES" -gt 1000 ]; then
            echo "⚠️ Large PR detected ($ADDED_LINES lines added). Consider breaking it down."
          else
            echo "✅ PR size looks good."
          fi